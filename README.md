Enable and disable all USB ports with one command (including camera, card reader, and hub)

**Build**
```
    sudo git clone https://github.com/osipenco/damn-usb
    cd damn-usb
    sudo make install
```

**Usage**
```
    sudo damnusb
```
